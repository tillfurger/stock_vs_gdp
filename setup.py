from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Does the stock market capitalization grow faster than the GDP?',
    author='team_stock_vs_gdp',
    license='MIT',
)
